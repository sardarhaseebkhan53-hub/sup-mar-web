import { useQuery, useQueryClient } from '@tanstack/react-query'; import { useEffect } from 'react'; import { useAuth } from '../auth/AuthProvider'; import { conversationApi } from '../services/apiClient'; import { getSocket } from '../services/socketClient';
export function useUnreadMessages(){const{user}=useAuth();const client=useQueryClient();const query=useQuery({queryKey:['conversation-unread'],enabled:Boolean(user),queryFn:async()=>(await conversationApi.list('page=1&limit=50')).data.unreadTotal as number,refetchInterval:30000});useEffect(()=>{if(!user)return;const socket=getSocket();const refresh=()=>void client.invalidateQueries({queryKey:['conversation-unread']});socket.on('conversation:updated',refresh);socket.on('message:new',refresh);socket.on('message:read',refresh);return()=>{socket.off('conversation:updated',refresh);socket.off('message:new',refresh);socket.off('message:read',refresh)}},[client,user]);return query.data||0}
