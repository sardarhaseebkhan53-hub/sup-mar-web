import { CalendarDays, MessageCircle, PackageCheck, Star } from 'lucide-react';import VerifiedBadge from './VerifiedBadge';export default function TrustIndicators({seller}:{seller:any}){return <div className="flex flex-wrap gap-2"><VerifiedBadge verified={seller?.verified||seller?.verificationStatus==='verified'}/>{seller?.memberSince&&<span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold"><CalendarDays size={12}/>Member since {new Date(seller.memberSince).getFullYear()}</span>}<span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold"><PackageCheck size={12}/>{seller?.soldListings||0} completed</span>{seller?.reviewCount>0&&<span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold"><Star size={12}/>{seller.rating} · {seller.reviewCount} reviews</span>}{seller?.responseLabel&&<span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold"><MessageCircle size={12}/>{seller.responseLabel}</span>}</div>}
