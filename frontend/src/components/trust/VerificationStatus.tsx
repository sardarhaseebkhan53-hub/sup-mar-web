import { BadgeCheck, CircleAlert, Clock3, FileQuestion, ShieldCheck, XCircle } from 'lucide-react';
export default function VerificationStatus({status='Not Started'}:{status?:string}){const normalized=status.toLowerCase();const Icon=normalized==='verified'?BadgeCheck:normalized.includes('reject')?XCircle:normalized.includes('information')?FileQuestion:normalized==='not started'?ShieldCheck:normalized==='expired'?CircleAlert:Clock3;const tone=normalized==='verified'?'border-emerald-200 bg-emerald-50 text-emerald-800':normalized.includes('reject')||normalized==='expired'?'border-rose-200 bg-rose-50 text-rose-800':normalized.includes('pending')||normalized.includes('review')||normalized.includes('information')?'border-amber-200 bg-amber-50 text-amber-900':'border-slate-200 bg-white text-slate-700';return <span className={`inline-flex items-center gap-2 rounded-full border px-3 py-1.5 text-[10px] font-extrabold ${tone}`}><Icon size={13}/>{status}</span>}
