import { MapPin, Tag } from 'lucide-react';
import type { ListingImage } from './ImageUploader';
interface Data { title: string; description: string; price: string; currency: string; negotiable: boolean; condition: string; categorySlug: string; subcategorySlug: string; location: { city: string; area: string }; images: ListingImage[]; }
export default function ListingPreview({ data }: { data: Data }) { return <div><h2 className="text-xl font-extrabold">Preview your listing</h2><p className="mt-1 text-sm text-slate-500">This is how buyers will see the essentials.</p><article className="mt-5 overflow-hidden rounded-panel border border-slate-200 bg-white shadow-card"><div className="grid md:grid-cols-2"><div className="aspect-[4/3] bg-slate-100">{data.images[0] ? <img src={data.images[0].url} alt="Listing cover preview" className="h-full w-full object-cover" /> : <div className="grid h-full place-items-center text-sm text-slate-400">No cover photo</div>}</div><div className="p-5 sm:p-7"><p className="text-[10px] font-extrabold uppercase tracking-wider text-violet-600">{data.categorySlug || 'Category'} · {data.subcategorySlug || 'General'}</p><h3 className="mt-2 text-2xl font-extrabold text-ink-950">{data.title || 'Your listing title'}</h3><p className="mt-4 text-2xl font-extrabold">Rs. {Number(data.price || 0).toLocaleString()} {data.negotiable && <span className="text-xs font-bold text-slate-500">· Negotiable</span>}</p><div className="mt-5 flex flex-wrap gap-2 text-xs font-bold text-slate-600"><span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-3 py-1.5"><Tag size={12} /> {data.condition || 'Condition'}</span><span className="inline-flex items-center gap-1 rounded-full bg-slate-100 px-3 py-1.5"><MapPin size={12} /> {[data.location.area, data.location.city].filter(Boolean).join(', ') || 'Location'}</span></div><div className="mt-6 border-t pt-5"><h4 className="text-sm font-extrabold">Description</h4><p className="mt-2 whitespace-pre-line text-sm leading-6 text-slate-600">{data.description || 'Your description will appear here.'}</p></div></div></div></article></div>; }
