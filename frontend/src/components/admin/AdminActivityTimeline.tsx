import { Activity } from 'lucide-react';
export default function AdminActivityTimeline({items=[]}:{items:any[]}){return <div className="space-y-0">{items.map(item=><article key={item.id} className="relative border-l-2 border-violet-100 pb-5 pl-5 last:pb-0"><span className="absolute -left-[7px] top-0 h-3 w-3 rounded-full bg-violet-500"/><h3 className="text-xs font-extrabold capitalize">{item.action.replaceAll('_',' ').toLowerCase()}</h3><p className="mt-1 text-[9px] text-slate-500">{item.targetType} {item.targetId} · {item.result||'success'}</p><time className="mt-1 block text-[9px] text-slate-400">{new Date(item.createdAt).toLocaleString()}</time></article>)}{!items.length&&<p className="flex items-center gap-2 p-4 text-xs text-slate-400"><Activity size={14}/>No administrative history.</p>}</div>}
