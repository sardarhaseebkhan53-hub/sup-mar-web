import { ShieldAlert } from 'lucide-react';export default function ViolationHistory({items=[]}:{items:any[]}){return <div className="space-y-2">{items.map(item=><div key={item.id} className="flex gap-3 rounded-xl bg-slate-50 p-3 text-xs"><ShieldAlert size={15} className="shrink-0 text-amber-600"/><div><strong>{item.type.replaceAll('_',' ')}</strong><p className="mt-1 text-[9px] text-slate-500">{item.action} · {item.targetId} · {new Date(item.createdAt).toLocaleDateString()}</p></div></div>)}{!items.length&&<p className="text-xs text-slate-400">No confirmed violation history.</p>}</div>}
