import type { ReactNode } from 'react';
export default function AdminTable({headers,children,empty}:{headers:string[];children:ReactNode;empty?:boolean}){return <div className="overflow-x-auto rounded-card border bg-white"><table className="w-full min-w-[760px] text-start"><thead className="bg-slate-50 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr>{headers.map(header=><th key={header} className="px-4 py-3">{header}</th>)}</tr></thead><tbody>{children}</tbody></table>{empty&&<p className="p-10 text-center text-xs text-slate-500">No records match these filters.</p>}</div>}
