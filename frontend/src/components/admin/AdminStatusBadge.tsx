export default function AdminStatusBadge({status}:{status:string}){const key=status.toLowerCase().replaceAll(' ','_');const color=['active','paid','published','resolved','verified','completed'].includes(key)?'bg-emerald-50 text-emerald-700':['pending','scheduled','processing','in_progress','waiting_for_user'].includes(key)?'bg-amber-50 text-amber-800':['failed','rejected','removed','suspended','restricted','cancelled','high'].includes(key)?'bg-rose-50 text-rose-700':'bg-slate-100 text-slate-600';return <span className={`inline-flex rounded-full px-2.5 py-1 text-[9px] font-extrabold capitalize ${color}`}>{status.replaceAll('_',' ')}</span>}
