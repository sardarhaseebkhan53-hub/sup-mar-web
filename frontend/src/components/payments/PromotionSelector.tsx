import { Check, Sparkles } from 'lucide-react';export default function PromotionSelector({products,value,onChange}:{products:any[];value:string;onChange:(value:string)=>void}){return <fieldset><legend className="text-sm font-extrabold">Choose a promotion</legend><div className="mt-4 grid gap-3 sm:grid-cols-2">{products.map(product=><button type="button" key={product.key} onClick={()=>onChange(product.key)} className={`relative rounded-card border p-5 text-left ${value===product.key?'border-violet-500 bg-violet-50 ring-2 ring-violet-500/10':'bg-white'}`}><Sparkles className="text-violet-600" size={20}/><h3 className="mt-3 text-sm font-extrabold">{product.name}</h3><p className="mt-2 text-xs text-slate-500">{product.durationHours<48?`${product.durationHours} hours`:`${Math.round(product.durationHours/24)} days`}</p><p className="mt-3 text-lg font-extrabold">Rs. {Number(product.price).toLocaleString()}</p>{value===product.key&&<Check className="absolute right-3 top-3 text-violet-600" size={17}/>}</button>)}</div></fieldset>}
