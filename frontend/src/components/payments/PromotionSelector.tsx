import { Check, Clock3, Sparkles } from 'lucide-react';
import { formatPrice } from '../../utils/formatters';

export default function PromotionSelector({products,value,onChange}:{products:any[];value:string;onChange:(value:string)=>void}){return <fieldset><legend className="text-sm font-extrabold">Choose a promotion</legend><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{products.map(product=><button type="button" key={product.key} onClick={()=>onChange(product.key)} aria-pressed={value===product.key} className={`relative rounded-card border p-5 text-left focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-violet-600 ${value===product.key?'border-violet-500 bg-violet-50 ring-2 ring-violet-500/10':'bg-white hover:border-violet-300'}`}><Sparkles className="text-violet-600" size={20}/><p className="mt-3 text-[9px] font-extrabold uppercase tracking-wider text-violet-600">{product.type.replace('_',' ')}</p><h3 className="mt-1 text-sm font-extrabold">{product.name}</h3>{product.description&&<p className="mt-2 min-h-8 text-[10px] leading-4 text-slate-500">{product.description}</p>}<p className="mt-3 flex items-center gap-1 text-xs text-slate-500"><Clock3 size={13}/>{product.durationHours<48?`${product.durationHours} hours`:`${Math.round(product.durationHours/24)} days`}</p><p className="mt-3 text-lg font-extrabold">{formatPrice(product.price,product.currency)}</p>{value===product.key&&<Check className="absolute right-3 top-3 text-violet-600" size={17}/>}</button>)}</div></fieldset>}
