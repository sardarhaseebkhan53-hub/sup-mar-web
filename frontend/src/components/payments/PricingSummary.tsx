export default function PricingSummary({baseAmount,tax,discount,total,currency='PKR'}:{baseAmount:number;tax:number;discount:number;total:number;currency?:string}){const amount=(value:number)=>`${currency==='PKR'?'Rs.':currency} ${Number(value).toLocaleString()}`;return <section className="rounded-panel border bg-white p-5"><h2 className="text-sm font-extrabold">Payment summary</h2><dl className="mt-4 space-y-3 text-xs"><div className="flex justify-between"><dt className="text-slate-500">Base fee</dt><dd className="font-bold">{amount(baseAmount)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Tax</dt><dd className="font-bold">{amount(tax)}</dd></div><div className="flex justify-between"><dt className="text-slate-500">Discount</dt><dd className="font-bold">− {amount(discount)}</dd></div><div className="flex justify-between border-t pt-4 text-base"><dt className="font-extrabold">Total</dt><dd className="font-extrabold">{amount(total)}</dd></div></dl></section>}
