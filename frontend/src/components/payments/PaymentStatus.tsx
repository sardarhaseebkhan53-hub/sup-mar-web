const tones:Record<string,string>={paid:'bg-emerald-50 text-emerald-700',pending:'bg-amber-50 text-amber-700',processing:'bg-blue-50 text-blue-700',failed:'bg-red-50 text-red-700',cancelled:'bg-slate-100 text-slate-600',refunded:'bg-violet-50 text-violet-700',expired:'bg-slate-100 text-slate-500'};export default function PaymentStatus({status}:{status:string}){return <span className={`inline-flex rounded-full px-2.5 py-1 text-[10px] font-extrabold capitalize ${tones[status]||tones.pending}`}>{status}</span>}
