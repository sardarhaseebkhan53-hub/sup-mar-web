import { useMutation } from '@tanstack/react-query'; import { Flag } from 'lucide-react'; import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import { useAuth } from '../../auth/AuthProvider'; import { listingApi } from '../../services/apiClient'; import { Modal } from '../ui/Modal'; import { Toast } from '../ui/Toast';
const reasons=[['scam','Scam or fraud'],['incorrect','Incorrect information'],['prohibited','Prohibited item'],['duplicate','Duplicate listing'],['offensive','Offensive content'],['wrong-category','Wrong category'],['other','Other']];
export default function ReportListing({id}:{id:string}){const{user}=useAuth();const navigate=useNavigate();const[open,setOpen]=useState(false);const[reason,setReason]=useState('');const[description,setDescription]=useState('');const[done,setDone]=useState(false);const mutation=useMutation({mutationFn:()=>listingApi.report(id,{reason,description}),onSuccess:()=>{setOpen(false);setDone(true)}});const launch=()=>user?setOpen(true):navigate(`/login?returnTo=${encodeURIComponent(location.pathname)}`);return <><button onClick={launch} className="inline-flex items-center gap-2 text-xs font-bold text-slate-500 hover:text-red-600"><Flag size={14}/>Report listing</button><Modal open={open} title="Report this listing" description="Help QAVLIO keep marketplace activity useful and safe." onClose={()=>setOpen(false)}><form onSubmit={(e)=>{e.preventDefault();if(reason)mutation.mutate()}}><fieldset><legend className="text-xs font-extrabold">Reason</legend><div className="mt-3 grid gap-2">{reasons.map(([value,label])=><label key={value} className="flex items-center gap-2 rounded-control border p-3 text-xs font-bold"><input type="radio" name="reason" value={value} checked={reason===value} onChange={()=>setReason(value)} required/>{label}</label>)}</div></fieldset><label className="mt-4 block text-xs font-extrabold">Details (optional)<textarea className="input-base mt-2 min-h-24 py-3" maxLength={1000} value={description} onChange={(e)=>setDescription(e.target.value)}/></label>{mutation.error&&<p role="alert" className="mt-3 text-xs font-bold text-red-600">{mutation.error.message}</p>}<button disabled={!reason||mutation.isPending} className="mt-5 h-11 w-full rounded-control bg-violet-600 text-xs font-extrabold text-white disabled:opacity-50">Submit report</button></form></Modal><Toast open={done} message="Report submitted for review." tone="success" onClose={()=>setDone(false)}/></>}
