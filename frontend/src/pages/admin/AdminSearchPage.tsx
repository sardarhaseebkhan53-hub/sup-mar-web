import DashboardLayout from '../../layouts/DashboardLayout';import AdminSearch from '../../components/admin/AdminSearch';import{useDocumentTitle}from'../../hooks/useDocumentTitle';export default function AdminSearchPage(){useDocumentTitle('Admin search');return <DashboardLayout role="admin"><header><p className="eyebrow">Command center</p><h1 className="mt-2 text-3xl font-extrabold">Global search</h1><p className="mt-2 text-sm text-slate-500">Search safe summaries across users, sellers, listings, orders, payments and reports.</p></header><div className="mt-6 max-w-3xl"><AdminSearch/></div></DashboardLayout>}
