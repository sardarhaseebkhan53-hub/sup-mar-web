export interface PublicSeller { username: string; displayName: string; description: string; avatar?: string | null; location: { country: string; province?: string; city?: string }; verificationStatus: string; rating: number; reviewCount: number; responseRate?: number | null; accountType: string; memberSince?: string; activeListings: number; soldListings: number; }
export interface PublicListing { publicId: string; slug: string; title: string; description: string; price: number | string; currency: 'PKR'; negotiable: boolean; condition: string; categorySlug?: string; subcategorySlug?: string; attributes?: Record<string, string | number | boolean>; media: Array<{ url: string; thumbnailUrl?: string; alt?: string; order: number; isCover?: boolean }>; coverImage?: string; location: { country: string; province?: string; city: string; area?: string }; status: string; viewCount?: number; favoriteCount?: number; createdAt: string; publishedAt?: string; sellerId?: string; seller?: PublicSeller | null; isOwner?: boolean; }
export const listingUrl = (listing: Pick<PublicListing, 'slug'|'publicId'>) => `/listing/${listing.slug}-${listing.publicId.toLowerCase()}`;
