# Runtime
NODE_ENV=development
PORT=5000
API_PREFIX=/api/v1

# Database — optional for the Phase 1 public foundation
MONGODB_URI=

# Authentication — Phase 2 must replace these with strong, random secrets
JWT_ACCESS_SECRET=replace-with-at-least-32-random-characters
JWT_REFRESH_SECRET=replace-with-a-different-strong-secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
JWT_REMEMBER_TTL=30d
OTP_PEPPER=replace-with-a-third-strong-random-secret
OTP_EXPIRES_MINUTES=10
OTP_RESEND_SECONDS=60
OTP_MAX_ATTEMPTS=5
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCK_MINUTES=15

# Browser origins; comma-separated
CLIENT_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Future providers (leave blank locally)
OTP_PROVIDER=
PAYMENT_PROVIDER=
MEDIA_PROVIDER=
AI_PROVIDER=

# One-time admin bootstrap (never expose to frontend; clear after use)
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_NAME=DealHub Administrator
