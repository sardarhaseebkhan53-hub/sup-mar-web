import mongoose from 'mongoose';const schema=new mongoose.Schema<any>({authorId:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true},targetType:{type:String,enum:['listing_report','chat_report','review_report','user_report','listing','user','seller','verification','appeal'],required:true},targetId:{type:String,required:true},note:{type:String,required:true,trim:true,maxlength:2000}},{timestamps:{createdAt:true,updatedAt:false}});schema.index({targetType:1,targetId:1,createdAt:-1});export const ModerationNote:mongoose.Model<any>=(mongoose.models.ModerationNote as mongoose.Model<any>)||mongoose.model<any>('ModerationNote',schema);
