import mongoose from 'mongoose';const schema=new mongoose.Schema<any>({userId:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true},adId:{type:mongoose.Schema.Types.ObjectId,ref:'Advertisement',required:true},rewardType:{type:String,required:true},rewardAmount:{type:mongoose.Schema.Types.Decimal128,required:true},status:{type:String,enum:['pending','verified','rejected'],default:'pending'}},{timestamps:{createdAt:true,updatedAt:false}});schema.index({userId:1,adId:1,createdAt:-1});export const RewardEvent:mongoose.Model<any>=(mongoose.models.RewardEvent as mongoose.Model<any>)||mongoose.model<any>('RewardEvent',schema);
