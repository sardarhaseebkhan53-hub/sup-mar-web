import mongoose from 'mongoose';const schema=new mongoose.Schema<any>({userId:{type:mongoose.Schema.Types.ObjectId,ref:'User',default:null,index:true},companyName:{type:String,required:true,trim:true,maxlength:160},contactEmail:{type:String,required:true,trim:true,lowercase:true},status:{type:String,enum:['pending','active','paused','rejected'],default:'pending',index:true}},{timestamps:true});export const Advertiser:mongoose.Model<any>=(mongoose.models.Advertiser as mongoose.Model<any>)||mongoose.model<any>('Advertiser',schema);
