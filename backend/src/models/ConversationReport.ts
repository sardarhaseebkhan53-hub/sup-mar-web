import mongoose from 'mongoose';const schema=new mongoose.Schema<any>({conversationId:{type:mongoose.Schema.Types.ObjectId,ref:'Conversation',required:true,index:true},reporterId:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true,index:true},reason:{type:String,enum:['scam','harassment','spam','prohibited','suspicious','other'],required:true,index:true},description:{type:String,trim:true,maxlength:1000,default:''},status:{type:String,enum:['pending','investigating','reviewed','resolved','rejected','escalated'],default:'pending',index:true},priority:{type:String,enum:['low','medium','high','critical'],default:'medium'},resolvedAt:Date,resolvedBy:{type:mongoose.Schema.Types.ObjectId,ref:'User',default:null}},{timestamps:true});schema.index({conversationId:1,reporterId:1,reason:1,status:1});schema.index({reporterId:1,createdAt:-1});schema.index({status:1,createdAt:-1});export const ConversationReport:mongoose.Model<any>=(mongoose.models.ConversationReport as mongoose.Model<any>)||mongoose.model<any>('ConversationReport',schema);
