import mongoose from 'mongoose';const schema=new mongoose.Schema<any>({adId:{type:mongoose.Schema.Types.ObjectId,ref:'Advertisement',required:true,index:true},type:{type:String,enum:['impression','click'],required:true},userId:{type:mongoose.Schema.Types.ObjectId,ref:'User',default:null},sessionId:{type:String,required:true},placement:{type:String,required:true},eventKey:{type:String,required:true,unique:true},timestamp:{type:Date,default:Date.now},metadata:{device:String,category:String,city:String}},{timestamps:false});schema.index({adId:1,type:1,timestamp:-1});schema.index({timestamp:-1});export const AdEvent:mongoose.Model<any>=(mongoose.models.AdEvent as mongoose.Model<any>)||mongoose.model<any>('AdEvent',schema);
