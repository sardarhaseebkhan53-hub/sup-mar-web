import mongoose from 'mongoose';const schema=new mongoose.Schema<any>({key:{type:String,default:'advertising',unique:true},dailyRewardLimit:{type:Number,min:0,default:3},rewardTypes:{type:[String],default:[]},impressionCooldownMinutes:{type:Number,min:1,default:30},clickCooldownSeconds:{type:Number,min:1,default:10}},{timestamps:true});export const AdSettings:mongoose.Model<any>=(mongoose.models.AdSettings as mongoose.Model<any>)||mongoose.model<any>('AdSettings',schema);
