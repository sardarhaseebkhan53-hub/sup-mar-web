import mongoose from 'mongoose';
const schema=new mongoose.Schema<any>({userId:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true,index:true},paymentId:{type:mongoose.Schema.Types.ObjectId,ref:'Payment',required:true,index:true},amount:{type:mongoose.Schema.Types.Decimal128,required:true},currency:{type:String,default:'PKR'},type:{type:String,enum:['charge','refund','adjustment'],required:true},status:{type:String,enum:['pending','completed','failed','refunded'],required:true},reference:{type:String,required:true,unique:true}},{timestamps:{createdAt:true,updatedAt:false}});schema.index({userId:1,createdAt:-1});schema.index({paymentId:1,type:1},{unique:true});
export const Transaction:mongoose.Model<any>=(mongoose.models.Transaction as mongoose.Model<any>)||mongoose.model<any>('Transaction',schema);
