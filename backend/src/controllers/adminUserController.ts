import { changeAccountStatus, changeRoles, getUserForAdmin, listUsers, resetVerification } from '../services/adminUserService.js';import { logAdminActivity } from '../services/adminActivityService.js';
export async function users(req,res){const data=await listUsers({...req.query,limit:1000});const page=Number(req.query.page)||1,limit=Number(req.query.limit)||25,start=(page-1)*limit;res.json({success:true,data:data.slice(start,start+limit),meta:{pagination:{page,limit,total:data.length,totalPages:Math.ceil(data.length/limit)}}})}export async function userDetails(req,res){res.json({success:true,data:await getUserForAdmin(req.params.id)})}export async function accountStatus(req,res){const data=await changeAccountStatus(req.auth.userId,req.params.id,req.body,req);await logAdminActivity(req.auth.userId,'ADMIN_UPDATED_USER_STATUS','user',req.params.id,{status:req.body.status,reason:req.body.reason},req);res.json({success:true,data,message:'Account status updated'})}export async function roles(req,res){const data=await changeRoles(req.auth.userId,req.params.id,req.body,req);await logAdminActivity(req.auth.userId,'ADMIN_UPDATED_USER_ROLES','user',req.params.id,{roles:req.body.roles},req);res.json({success:true,data,message:'Account roles updated'})}export async function verification(req,res){const data=await resetVerification(req.auth.userId,req.params.id,req.body,req);await logAdminActivity(req.auth.userId,'ADMIN_UPDATED_USER_VERIFICATION','user',req.params.id,{type:req.body.type,status:req.body.status},req);res.json({success:true,data,message:'Verification updated'})}
