export const USER_ROLES = Object.freeze({
  CUSTOMER: 'customer',
  SELLER: 'seller',
  ADMIN: 'admin',
  MODERATOR: 'moderator',
  SUPPORT: 'support',
});

export const ROLE_VALUES = Object.values(USER_ROLES);
