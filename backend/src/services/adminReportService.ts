import{AppError}from'../utils/AppError.js';import{adminListingReports,adminUpdateListingReport}from'./reportService.js';import{adminConversationReports,adminUpdateConversationReport}from'./messagingService.js';import{addModerationNote,getModerationNotes,logAdminActivity}from'./adminActivityService.js';const rank=(value:string)=>({critical:4,high:3,medium:2,low:1} as Record<string,number>)[value]||0;const priority=(reason:string)=>['scam','prohibited'].includes(reason)?'critical':['harassment','suspicious'].includes(reason)?'high':['spam','offensive'].includes(reason)?'medium':'low';const present=(item:any,type:string)=>({id:String(item._id||item.id),type,targetId:String(item.listingId||item.conversationId),reporterId:String(item.reporterId),reason:item.reason,description:item.description||'',status:item.status,priority:priority(item.reason),createdAt:item.createdAt});export async function adminReports(input:any){const rows=[...(await adminListingReports()).map((x:any)=>present(x,'listing')),...(await adminConversationReports()).map((x:any)=>present(x,'chat'))].filter(item=>(!input.type||item.type===input.type)&&(!input.status||item.status===input.status)&&(!input.priority||item.priority===input.priority)&&(!input.search||`${item.id} ${item.targetId} ${item.reason}`.toLowerCase().includes(input.search.toLowerCase()))).sort((a,b)=>(rank(b.priority)-rank(a.priority))||+new Date(b.createdAt)-+new Date(a.createdAt));const total=rows.length,start=(input.page-1)*input.limit;return{reports:rows.slice(start,start+input.limit),pagination:{page:input.page,limit:input.limit,total,totalPages:Math.ceil(total/input.limit)},summary:{pending:rows.filter(x=>x.status==='pending').length,critical:rows.filter(x=>x.priority==='critical'&&['pending','investigating','escalated'].includes(x.status)).length}}}export async function adminReportDetail(id:string){const all=(await adminReports({page:1,limit:10000})).reports,item=all.find(report=>report.id===id);if(!item)throw new AppError(404,'Report not found','REPORT_NOT_FOUND');return{...item,notes:await getModerationNotes(item.type==='chat'?'chat_report':'listing_report',id)}}export async function updateAdminReport(adminId:string,id:string,status:string,note:string|undefined,req:any){const detail=await adminReportDetail(id);const result=detail.type==='chat'?await adminUpdateConversationReport(id,status):await adminUpdateListingReport(id,status);if(note)await addModerationNote(adminId,detail.type==='chat'?'chat_report':'listing_report',id,note);await logAdminActivity(adminId,`ADMIN_${status.toUpperCase()}_REPORT`,'report',id,{type:detail.type},req);return{...present(result,detail.type),notes:await getModerationNotes(detail.type==='chat'?'chat_report':'listing_report',id)}}
