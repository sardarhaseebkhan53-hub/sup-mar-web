import mongoose from'mongoose';import{PlatformSetting}from'../models/PlatformSetting.js';import{MarketplaceSettings}from'../models/MarketplaceSettings.js';import{getMarketplaceSettings}from'./marketplaceSettingsService.js';import{AppError}from'../utils/AppError.js';import{logAdminActivity}from'./adminActivityService.js';const memory=new Map<string,any>();const definitions:any={freeListingLimit:{category:'marketplace',type:'number',description:'Number of listings published without a fee',min:0,max:100},additionalListingFee:{category:'payments',type:'number',description:'Fee for additional listings in PKR',min:0,max:100000},promotionEnabled:{category:'promotions',type:'boolean',description:'Allow paid listing promotions'},maximumImages:{category:'listings',type:'number',description:'Maximum images per listing',min:1,max:30},listingExpirationDays:{category:'listings',type:'number',description:'Default listing expiration in days',min:1,max:365},dailyRewardLimit:{category:'advertisements',type:'number',description:'Daily pending reward claim limit',min:0,max:100},moderationRequired:{category:'moderation',type:'boolean',description:'Send new listings to moderation'},messageNotifications:{category:'notifications',type:'boolean',description:'Enable message notifications'},sessionSecurityEnabled:{category:'security',type:'boolean',description:'Enforce active session validation'}};const defaults:any={maximumImages:12,listingExpirationDays:30,dailyRewardLimit:3,moderationRequired:false,messageNotifications:true,sessionSecurityEnabled:true};export async function adminSettings(){const marketplace:any=await getMarketplaceSettings();let stored:any[]=mongoose.connection.readyState===1?await PlatformSetting.find({key:{$in:Object.keys(definitions)}}).lean():[...memory.values()];const values:any={...defaults,freeListingLimit:marketplace.freeListingLimit,additionalListingFee:marketplace.additionalListingFee,promotionEnabled:marketplace.promotionEnabled};for(const item of stored)values[item.key]=item.value;return Object.entries(definitions).map(([key,definition]:any)=>({key,value:values[key],...definition,updatedAt:stored.find(item=>item.key===key)?.updatedAt||null}))}export async function updateAdminSetting(adminId:string,key:string,value:any,req:any){const definition=definitions[key];if(!definition)throw new AppError(404,'Setting not found','SETTING_NOT_FOUND');if(['payments','security'].includes(definition.category)&&!req.auth?.roles?.includes('super_admin'))throw new AppError(403,'Only a super administrator can change this setting','SUPER_ADMIN_REQUIRED');if(definition.type==='number'&&(typeof value!=='number'||!Number.isFinite(value)||value<definition.min||value>definition.max))throw new AppError(422,`Value must be between ${definition.min} and ${definition.max}`,'SETTING_VALUE_INVALID');if(definition.type==='boolean'&&typeof value!=='boolean')throw new AppError(422,'Value must be true or false','SETTING_VALUE_INVALID');if(['freeListingLimit','additionalListingFee','promotionEnabled'].includes(key)){if(mongoose.connection.readyState===1){const patch=key==='additionalListingFee'?{additionalListingFee:value}:{[key]:value};await MarketplaceSettings.findOneAndUpdate({key:'marketplace'},{$set:patch,$setOnInsert:{currency:'PKR'}},{upsert:true,new:true})}else{const module:any=await import('./marketplaceSettingsService.js');if(module.__setMemoryMarketplaceSetting)module.__setMemoryMarketplaceSetting(key,value)}}const record={key,value,type:definition.type,category:definition.category,description:definition.description,updatedBy:adminId,updatedAt:new Date()};if(mongoose.connection.readyState===1)await PlatformSetting.findOneAndUpdate({key},{$set:record},{upsert:true});else memory.set(key,record);await logAdminActivity(adminId,'ADMIN_UPDATED_SETTING','setting',key,{value},req);return record}
