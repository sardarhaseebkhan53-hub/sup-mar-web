import{getIdentityRepository}from'../repositories/identityRepository.js';import{getSellerProfileRepository}from'../repositories/sellerProfileRepository.js';import{adminListingMetrics,adminListListings}from'./listingService.js';import{adminListPayments,adminListPromotions}from'./paymentService.js';import{analyticsSeries}from'./advertisementService.js';import{adminReports}from'./adminReportService.js';export async function adminDashboard(days=30){const[users,sellers,listing,payments,promotions,ads,reports]=await Promise.all([getIdentityRepository().listUsers({limit:10000}),getSellerProfileRepository().list({limit:10000}),adminListingMetrics(),adminListPayments({page:1,limit:10000}),adminListPromotions({page:1,limit:10000}),analyticsSeries(),adminReports({page:1,limit:10000})]);const paid=payments.payments.filter((item:any)=>item.status==='paid'),revenue=paid.reduce((sum:number,item:any)=>sum+Number(item.amount),0),start=Date.now()-days*86400000;const series=new Map<string,any>();const row=(date:any)=>{const key=new Date(date).toISOString().slice(0,10),value=series.get(key)||{date:key,users:0,listings:0,transactions:0,revenue:0,reports:0,adImpressions:0};series.set(key,value);return value};for(const item of users)if(+new Date(item.createdAt)>=start)row(item.createdAt).users++;for(const item of listing.rows)if(+new Date(item.createdAt)>=start)row(item.createdAt).listings++;for(const item of paid)if(+new Date(item.createdAt)>=start){row(item.createdAt).transactions++;row(item.createdAt).revenue+=Number(item.amount)}for(const item of reports.reports)if(+new Date(item.createdAt)>=start)row(item.createdAt).reports++;for(const item of ads.series)if(+new Date(item.date)>=start)row(item.date).adImpressions+=item.impressions;return{metrics:{totalUsers:users.length,totalSellers:sellers.length,activeListings:listing.active,pendingListings:listing.pending,soldListings:listing.sold,totalTransactions:paid.length,revenue,activePromotions:promotions.promotions.filter((x:any)=>x.status==='active').length,activeAdvertisements:ads.summary.active,pendingReports:reports.reports.filter((x:any)=>['pending','investigating','escalated'].includes(x.status)).length},series:[...series.values()].sort((a,b)=>a.date.localeCompare(b.date))}}
export async function adminAnalyticsData(input:any){const dashboard=await adminDashboard(input.days||30),listings=await adminListListings({page:1,limit:10000,sort:'views'});const categoryMap=new Map<string,any>();for(const item of listings.listings){const key=item.categorySlug||'uncategorized',row=categoryMap.get(key)||{category:key,listings:0,views:0,favorites:0,sales:0};row.listings++;row.views+=item.viewCount||0;row.favorites+=item.favoriteCount||0;if(item.status==='sold')row.sales++;categoryMap.set(key,row)}return{...dashboard,topCategories:[...categoryMap.values()].sort((a,b)=>b[input.sort||'listings']-a[input.sort||'listings']).slice(0,10),topListings:listings.listings.slice(0,10)}}
export async function globalAdminSearch(query:string){if(!query.trim())return{users:[],sellers:[],listings:[],payments:[],reports:[]};const[users,sellers,listings,payments,reports]=await Promise.all([getIdentityRepository().listUsers({search:query,limit:10}),getSellerProfileRepository().list({search:query,limit:10}),adminListListings({search:query,page:1,limit:10,sort:'newest'}),adminListPayments({search:query,page:1,limit:10}),adminReports({search:query,page:1,limit:10})]);return{users:users.slice(0,10),sellers:sellers.slice(0,10),listings:listings.listings,payments:payments.payments,reports:reports.reports}}
