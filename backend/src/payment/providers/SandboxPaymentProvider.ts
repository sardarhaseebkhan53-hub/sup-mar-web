import crypto from 'node:crypto'; import { env } from '../../config/env.js'; import type { PaymentProvider, ProviderCreateInput, ProviderPayment, WebhookEvent } from '../PaymentProvider.js';
const records=new Map<string,ProviderPayment>();
export class SandboxPaymentProvider implements PaymentProvider{name='sandbox';async createPayment(_input:ProviderCreateInput){const item:ProviderPayment={providerPaymentId:`sandbox_${crypto.randomUUID()}`,status:'pending'};records.set(item.providerPaymentId,item);return item}async verifyPayment(id:string){const item=records.get(id)||{providerPaymentId:id,status:'pending' as const};const paid={...item,status:'paid' as const};records.set(id,paid);return paid}async refundPayment(id:string,_amount:number){const item={providerPaymentId:id,status:'refunded' as const};records.set(id,item);return item}async getPaymentStatus(id:string){return records.get(id)||{providerPaymentId:id,status:'pending'}}verifyWebhook(raw:string,signature:string):WebhookEvent{const expected=crypto.createHmac('sha256',env.commerce.paymentWebhookSecret).update(raw).digest('hex');if(!signature||signature.length!==expected.length||!crypto.timingSafeEqual(Buffer.from(signature),Buffer.from(expected)))throw new Error('Invalid webhook signature');return JSON.parse(raw)}}
